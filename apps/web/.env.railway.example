# Copiar estas variables al servicio WEB (CRM) en Railway (Settings → Variables)
# Reemplaza la URL example por la de tu servicio API en Railway

NEXT_PUBLIC_API_URL=https://eventos-api-production.up.railway.app

NODE_ENV=production
